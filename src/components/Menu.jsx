import styled from 'styled-components';

const Menu = () => {
  return (
    <Container>
      Menu
    </Container>
  );
};

const Container = styled.div`
  flex: 1;
  background-color: #202020;
`;

export default Menu;
