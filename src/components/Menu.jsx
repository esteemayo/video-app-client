import styled from 'styled-components';

const Menu = () => {
  return (
    <Container>
      Menu
    </Container>
  );
};

const Container = styled.div`
  flex: 1;
`;

export default Menu;
